version: 1.0
# Must include runtime: 'container' but no unsupported fields
runtime: container

build:
  dockerfile: Dockerfile
  context: .

run:
  command: gunicorn --bind 0.0.0.0:5000 application:app
  port: 5000
